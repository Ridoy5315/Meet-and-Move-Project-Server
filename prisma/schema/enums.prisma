enum UserRole {
  SUPER_ADMIN
  ADMIN
  HOST
  USER
}

enum UserStatus {
  ACTIVE
  BLOCKED
  SUSPENDED
}

enum Gender {
  MALE
  FEMALE
}

enum AuthProvider {
  LOCAL
  GOOGLE
}

enum HostStatus {
  PENDING
  APPROVED
  REJECTED
}

enum EventCategory {
  EVENT
  ACTIVITY
}

enum PriceType {
  FREE
  PAID
}

enum EventStatus {
  PENDING
  PUBLISHED
  REJECTED
}