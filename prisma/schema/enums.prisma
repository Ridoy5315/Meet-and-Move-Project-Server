enum UserRole {
  SUPER_ADMIN
  ADMIN
  HOST
  USER
}

enum UserStatus {
  ACTIVE
  BLOCKED
  SUSPENDED
}

enum Gender {
  MALE
  FEMALE
}

enum AuthProvider {
  LOCAL
  GOOGLE
}

enum HostStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PriceType {
  FREE
  PAID
}

enum EventApprovalStatus {
  PENDING
  PUBLISHED
  REJECTED
}

enum EventLifecycleStatus {
  UPCOMING              // registration open OR not started yet (before deadline)
  REGISTRATION_CLOSED   // ✅ NEW: after registrationDeadline but before event starts
  ONGOING               // event started
  COMPLETED             // event ended
  CANCELLED
}
